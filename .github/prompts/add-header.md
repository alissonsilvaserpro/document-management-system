---
description: Adicione header em todos os arquivos .js
name: add-header
agent: agent
---

# Adicione headers

Abra todos os arquivos .js `${input:modulo:caminho do modulo}` e adicione um header (topo do arquivo) explicando sobre o que esse arquivo faz, de forma resumida.
